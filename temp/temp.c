#include <stdio.h>
#include <string.h>

int main() {
    char str[5] = {'H', 'E', 'L', 'L', 'O'};
    int len = strlen(str);
    printf("length of string %d \n", len);
    printf("string: %s \n", str);
}